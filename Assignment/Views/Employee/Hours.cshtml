@model Dictionary<string, double>

<!DOCTYPE html>
<html>
<head>
    <title>Hours</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #4CAF50;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        .red {
            color: white;
            background-color: red;
            font-weight: bold;
        }

        .chart-container {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            height: 400px;
            margin-top: 30px; /* Increased margin-top to 30px */
        }
    </style>
</head>
<body>
    <table>
        <thead>
            <tr>
                @* <th>Serial Number</th> *@
                <th>EmployeeName</th>
                <th>Total working hours</th>
            </tr>
        </thead>
        <tbody>
            @{
                // int serialNumber = 1;
            }
            @foreach (var item in Model)
            {
                <tr>
                    @* <td>@serialNumber</td> *@
                    <td>@item.Key</td>
                    <td class="@(item.Value < 100 ? "red" : "")">@Math.Round(item.Value, 2)</td>
                </tr>
                @* serialNumber++; *@
            }
        </tbody>
    </table>
    <div class="chart-container"></div>
</body>
</html>
